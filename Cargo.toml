[package]
name = "expr"
version = "0.1.0"
edition = "2024"
description = "A runtime for manipulating expressions in a natural deduction style"

[dependencies]
map-in-place = { path = "../map-in-place" }
