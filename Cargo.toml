[package]
name = "expr"
version = "0.1.0"
edition = "2024"
description = "Facilities for building and comparing expression trees of tokens"

[dependencies]
map-in-place = { path = "../map-in-place" }
sparse-vec = { path = "../sparse-vec" }
